{% extends 'base.html.twig' %}

{% block title %}Quiz{% endblock %}

{% block body %}
    <h1>Quiz</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdQuiz</th>
                <td>{{ quiz.idQuiz }}</td>
            </tr>
            <tr>
                <th>Categories</th>
                <td>{{ quiz.categories }}</td>
            </tr>
            <tr>
                <th>Quiz</th>
                <td>{{ quiz.quiz }}</td>
            </tr>
            <tr>
                <th>Resultat</th>
                <td>{{ quiz.resultat }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ quiz.duree }}</td>
            </tr>
            <tr>
                <th>DateCreation</th>
                <td>{{ quiz.dateCreation ? quiz.dateCreation|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('quiz_index') }}">back to list</a>

    <a href="{{ path('quiz_edit', {'idQuiz': quiz.idQuiz}) }}">edit</a>

    {{ include('quiz/_delete_form.html.twig') }}
{% endblock %}
